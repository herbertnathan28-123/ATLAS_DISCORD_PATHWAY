node_modules/
npm-debug.log*
.env
.env.local
.DS_Store
.cache/

# ATLAS Foundation Repair v1.0.1 — doctrine harness output (regenerated each run)
audit.json
audit.summary.md
runtime-packet-test.json
runtime-packet-test.summary.md

# Puppeteer browser cache (configured in .puppeteerrc.cjs)
.cache/

# ATLAS Phase D — local historical cache (Render-only persistent disk in production)
data/historical/
data/historical/**
